<UserControl x:Class="KMR.View.Calculation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:local="clr-namespace:KMR.Properties"
             xmlns:kmr="clr-namespace:KMR.View"
             xmlns:common="clr-namespace:KMR.Common"
             Width="{Binding Width, ElementName=PageSwitcher}"
             Height="{Binding Height, ElementName=PageSwitcher}" 
             >
    <UserControl.Resources>
        <!--<Style TargetType="Label" BasedOn="{StaticResource CalcCaptions}"/>-->
    </UserControl.Resources>
    
    <DockPanel LastChildFill="True">
        <Menu DockPanel.Dock="Top" Height="50" VerticalAlignment="Top">
            <Button Command="common:Commands.OpenMenuView">Zurück</Button>
        </Menu>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="514*"></ColumnDefinition>
                    <ColumnDefinition Width="486*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <kmr:SubstanceLabels>
                </kmr:SubstanceLabels>
            </Grid>
        </ScrollViewer>
    </DockPanel>
</UserControl>